{
  "name": "railnexus",
  "version": "1.0.0",
  "description": "RailNexus — Railway Telecom Maintenance System (monorepo)",
  "private": true,
  "scripts": {
    "dev": "concurrently -n BACKEND,FRONTEND -c \"bgBlue.bold,bgGreen.bold\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "seed": "npm run seed --prefix backend",
    "install:all": "npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
